if ($(window).width() > 750) {
    $(".scrollhere").niceScroll({
        cursorcolor: "#ccc",
        preventmultitouchscrolling: true
    });  

    $(".scrollola").niceScroll({
        cursorcolor: "#ccc",
        preventmultitouchscrolling: true
    }); 
 }
